spring:
    application:
        name: auth-service
server:
    port: 8083
eureka:
    client:
        serviceUrl:
            defaultZone: ${EUREKA_URI:http://localhost:8081/eureka}
    instance:
        preferIpAddress: true